name: Deploy Dev (Dummy)

on:
  workflow_dispatch:

jobs:
  call-dummy:
    uses: ./.github/workflows/deploy.yml
    with:
      target-env: dev
    secrets:
      DUMMY_SECRET: ${{ secrets.DUMMY_SECRET }}
    permissions:
      id-token: write      
      contents: read
